# Backend FastAPI compatible PostgreSQL et SQL Server

## Installation

Créer un environnement virtuel (recommandé) :

```bash
python -m venv venv
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows
```
